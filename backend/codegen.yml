overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
generates:
  src/@types/generated-types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
